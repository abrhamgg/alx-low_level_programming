#include <stdio.h>

/**
 * main - starting point
 *
 * return: 0 if the code runs without error
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
