#include "main.h"

/**
 * _isdigit - check the code.
 * @c: Variable text
 * Return: check the letter and return 1 if its digit, 0 otherwise.
 */

int _isdigit(int c)
{
        if (c >= 0 && c <= 9)
        {
        return (1);
        }
        else
        {
        return (0);
        }
}

