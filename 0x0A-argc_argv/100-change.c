#include <stdio.h>
#include <string.h>
#include <stdlib.h>

/**
 * main - function that prints minimum number of
 * coins to make change for amount.
 * @argc: no of argument.
 * @argv: array of arguments.
 * Return: 0 (Success);
 */

int main(int argc, char *argv[])
{
	int c = atoi(argv[1]);
	int count = 0;
}
